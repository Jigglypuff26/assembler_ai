# «Приветствую Омниссию! Он — Бог в Машине, Источник Всех Знаний». 

Assembler AI - Нейросеть на ассемблере
🧠 Описание проекта

Assembler AI - это полноценная нейронная сеть, написанная на ассемблере x86-64 с использованием AVX инструкций для векторной оптимизации. Проект демонстрирует работу нейросетей на самом низком уровне, обеспечивая максимальную производительность и глубокое понимание внутренних механизмов.

Основные возможности:

    Полносвязные нейронные сети (Dense layers)

    Поддержка функций активации: ReLU, Sigmoid, Tanh

    Алгоритм обратного распространения ошибки

    SGD оптимизатор с momentum

    Векторизация с помощью AVX инструкций

    Решение задачи XOR

🏗️ Архитектура проекта
text

assembler_ai/
├── src/
│   ├── core/               # Базовые операции
│   │   ├── memory.asm      # Управление памятью
│   │   ├── math.asm        # Математические операции
│   │   └── linear.asm      # Линейная алгебра
│   ├── layers/             # Слои нейросети
│   │   ├── dense.asm       # Полносвязный слой
│   │   └── activation.asm  # Функции активации
│   ├── loss/               # Функции потерь
│   │   └── mse.asm         # Среднеквадратичная ошибка
│   └── optim/              # Оптимизаторы
│       └── sgd.asm         # Stochastic Gradient Descent
├── include/                # Заголовочные файлы
│   ├── constants.inc       # Константы и макросы
│   └── macros.inc          # Вспомогательные макросы
├── main.asm               # Главная программа
└── README.md              # Документация

🎯 Архитектура нейросети

Проект реализует сеть для задачи XOR:
text

Входной слой (2 нейрона)
    ↓
Скрытый слой (4 нейрона, ReLU)
    ↓
Выходной слой (1 нейрон, линейная активация)

⚙️ Требования для запуска
Для Linux:

    Ассемблер: NASM (Netwide Assembler) версии 2.13+

    Компьютер: x86-64 архитектура с поддержкой AVX

    ОС: Любой дистрибутив Linux

Установка NASM на Linux:
bash

# Ubuntu/Debian
sudo apt-get install nasm

# CentOS/RHEL/Fedora
sudo yum install nasm
# или
sudo dnf install nasm

# Arch Linux
sudo pacman -S nasm

Для Windows:

    Ассемблер: NASM для Windows

    Компьютер: x86-64 архитектура с поддержкой AVX

    Дополнительно: WSL2 (рекомендуется) или MinGW

# Установка на Windows:

Вариант 1: WSL2 (рекомендуется)
bash

# Установите WSL2 с Ubuntu из Microsoft Store
# Затем в WSL:
sudo apt-get update
sudo apt-get install nasm

Вариант 2: Нативное Windows

    Скачайте NASM с https://www.nasm.us/

    Распакуйте в C:\nasm\

    Добавьте C:\nasm\ в PATH

    Используйте командную строку или PowerShell

🚀 Запуск проекта
На Linux:

    Клонируйте репозиторий:

bash

git clone https://github.com/Jigglypuff26/assembler_ai.git

cd assembler_ai

    Компиляция всех модулей:

bash

# Компиляция основных модулей
nasm -f elf64 src/core/memory.asm -o memory.o

nasm -f elf64 src/core/math.asm -o math.o

nasm -f elf64 src/core/linear.asm -o linear.o

nasm -f elf64 src/layers/dense.asm -o dense.o

nasm -f elf64 src/layers/activation.asm -o activation.o

nasm -f elf64 src/loss/mse.asm -o mse.o

nasm -f elf64 src/optim/sgd.asm -o sgd.o

nasm -f elf64 main.asm -o main.o

    Линковка и запуск:

bash

# Линковка
ld -static -o neural_net *.o

# Запуск
./neural_net

На Windows:

Через WSL2 (рекомендуется):
bash

# Выполните те же команды, что и для Linux

Нативное выполнение:
cmd

:: Компиляция
nasm -f win64 src/core/memory.asm -o memory.obj
nasm -f win64 src/core/math.asm -o math.obj
nasm -f win64 src/core/linear.asm -o linear.obj
nasm -f win64 src/layers/dense.asm -o dense.obj
nasm -f win64 src/layers/activation.asm -o activation.obj
nasm -f win64 src/loss/mse.asm -o mse.obj
nasm -f win64 src/optim/sgd.asm -o sgd.obj
nasm -f win64 main.asm -o main.obj

:: Линковка (требуется Microsoft Linker)
link /subsystem:console /entry:main *.obj

:: Запуск
neural_net.exe

📝 Makefile для упрощения сборки

Создайте файл Makefile:
makefile

ASM = nasm
LD = ld
ASM_FLAGS = -f elf64
LD_FLAGS = -static

SOURCES = main.asm \
          src/core/memory.asm \
          src/core/math.asm \
          src/core/linear.asm \
          src/layers/dense.asm \
          src/layers/activation.asm \
          src/loss/mse.asm \
          src/optim/sgd.asm

OBJECTS = $(SOURCES:.asm=.o)

TARGET = neural_net

all: $(TARGET)

$(TARGET): $(OBJECTS)
	$(LD) $(LD_FLAGS) -o $@ $^

%.o: %.asm
	$(ASM) $(ASM_FLAGS) $< -o $@

clean:
	rm -f $(OBJECTS) $(TARGET)

run: $(TARGET)
	./$(TARGET)

.PHONY: all clean run

Использование Makefile:
bash

make        # Сборка проекта
make run    # Сборка и запуск
make clean  # Очистка

📊 Ожидаемый вывод

После запуска вы должны увидеть:
text

Training XOR network...
Epoch: 100
Epoch: 200
...
Results:
Input: 0.0 0.0 Output: 0.05 Target: 0.0
Input: 0.0 1.0 Output: 0.92 Target: 1.0
Input: 1.0 0.0 Output: 0.94 Target: 1.0
Input: 1.0 1.0 Output: 0.08 Target: 0.0

🔧 Возможные проблемы и решения
Ошибка: "instruction not supported"

    Причина: Процессор не поддерживает AVX инструкции

    Решение: Проверьте поддержку AVX или используйте эмуляцию

Ошибка линковки на Windows

    Причина: Отсутствует подходящий линкер

    Решение: Используйте WSL2 или установите Microsoft Visual Studio Build Tools

NASM не найден

    Решение: Убедитесь, что NASM установлен и добавлен в PATH

🎯 Образовательная ценность

Этот проект поможет вам понять:

    Принципы работы нейронных сетей на низком уровне

    Оптимизацию вычислений с помощью AVX инструкций

    Организацию памяти и управление ресурсами

    Алгоритмы обратного распространения ошибки

    Векторизацию математических операций

📈 Дальнейшее развитие

    Добавить поддержку сверточных слоев (CNN)

    Реализовать рекуррентные сети (RNN/LSTM)

    Добавить сохранение и загрузку моделей

    Реализовать различные оптимизаторы (Adam, RMSprop)

    Добавить поддержку GPU вычислений

Автор: Jigglypuff26
Лицензия: MIT License